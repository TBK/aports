--- a/third-party/webscalesqlclient/mysql-5.6/cmake/ssl.cmake
+++ b/third-party/webscalesqlclient/mysql-5.6/cmake/ssl.cmake
@@ -123,8 +123,7 @@
 
     IF(OPENSSL_INCLUDE_DIR AND
        OPENSSL_LIBRARY   AND
-       CRYPTO_LIBRARY      AND
-       OPENSSL_MAJOR_VERSION STREQUAL "1"
+       CRYPTO_LIBRARY
       )
       SET(OPENSSL_FOUND TRUE)
     ELSE()
